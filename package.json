{
  "name": "playwright-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Testes automatizados em Playwright (API + fluxo de cadastro/compra)",
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test tests/api",
    "test:cadastro": "playwright test tests/cadastro",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
